HTML
Basics of html.
All HTML documents must start with <!DOCTYPE HTML>
The HTML document itself begins with <html> and ends with </html>.
All html tags consist of a start tag and an end tag.
The visible part of the HTML document is between <body> and </body>.
HTML headings are defined with <h1> to <h6> tags.
HTML paragraphs are defined with <p> tag.
HTML links are defined with <a> tag.
The links destination is specified in the href attribute.
HTML images are defined with the <img> tag.
HTML buttons are defined with < button> tag.
HTML lists are defined with <ul> tag for unordered lists and <ol> tag for ordered list followed by <li> tag.
<br> is an empty element without a closing tag.
